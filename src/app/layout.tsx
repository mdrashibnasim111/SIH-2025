//This file is intentionally left blank. It is not needed for i18n routing.
//The main layout is in src/app/[locale]/layout.tsx
export default function RootLayout({children}: {children: React.ReactNode}) {
  return children;
}
